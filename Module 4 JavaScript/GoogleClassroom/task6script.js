// Create a variable called name and assign your name to it.
let name = "John Doe";

// Use string concatenation to create a greeting message.
let greeting = "Hello, " + name + "!";
console.log(greeting);  // Prints: Hello, John Doe!

// Create a variable called sentence and assign any sentence to it.
let sentence = "This is a sample sentence for testing.";
// Use the length property to determine how many characters are in the sentence.
console.log(sentence.length);  // Prints the number of characters

// Create a variable called str and assign any string to it.
let str = "Hello, world!";
// Convert str to uppercase using the toUpperCase() method.
let upperStr = str.toUpperCase();
console.log(upperStr);  // Prints: HELLO, WORLD!
